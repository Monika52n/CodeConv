import java.util.*;

class Main {
    public static void main(String[] args) {
        List<Integer> students = Arrays.asList("John Doe", "Jane Smith", "Alice Johnson", "Bob Brown", "Charlie Davis");
        List<Integer> finalStudents = new ArrayList<>();
        finalStudents.addAll(students);
        finalStudents.forEach(student -> System.out.println("Students sorted by age: " + student.get(a, b))));
    }
}